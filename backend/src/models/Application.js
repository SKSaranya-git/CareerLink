// Kept for backwards compatibility with older imports.
// The canonical Application model now lives in `src/modules/application/application.model.js`.
module.exports = require("../modules/application/application.model");
